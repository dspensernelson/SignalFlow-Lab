**Stepper** — horizontal progress pills for the three-step lesson flow.

```jsx
<Stepper steps={['Intro', 'Exercise', 'Takeaway']} current={1} />
```

Steps before `current` show a check; the `current` step is highlighted blue; later steps are muted.
