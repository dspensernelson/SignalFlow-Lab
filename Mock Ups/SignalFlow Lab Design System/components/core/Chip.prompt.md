**Chip** — a muted tag. Use for lesson concept lists; `mono` for field/file names.

```jsx
<Chip>JSON</Chip> <Chip>Schema</Chip> <Chip>Field extraction</Chip>
<Chip mono>market-intake.json</Chip>
```
